// Ejercicio 1: Saludo personalizado
function saludar(nombre) {
return `¡Hola, ${nombre}!`;
}
let resultado = saludar("Thaís");
console.log(resultado);

// Ejercicio 2: Suma de dos números
function suma(a, b) {
  return a + b;
}
console.log(suma(1989, 100));

// Ejercicio 3: Números pares Escribe una función llamada esPar que reciba un número y devuelva true si el número es par o false si es impar.
function esPar (num) {
    return num % 2 === 0;
}
console.log(esPar(1));
console.log(esPar(10));
console.log(esPar(26));
console.log(esPar(49));

// Ejercicio 4: Números pares Escribe una función llamada esPar que reciba un número y devuelva true si el número es par o false si es impar.